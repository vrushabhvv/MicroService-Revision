server:
  port: 8085

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vrushabhvv/Microservices_Configurations
          clone-on-start: true #this property will clone above repository during application start up, initially it will take time to load but the querieng is very speed, querieng:-> hitting url
